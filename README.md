# linux-server-toolkit
A toolkit for sysadmins to use on linux servers

Run `./server_toolkit.py` from the root directory to start

`-c / --cli` to enter cli mode

`-t / --tui` to enter tui mode

I plan to write this purely in bash at some point
